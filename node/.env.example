# ===========================================
# 答题系统环境变量配置模板
# 复制此文件为 .env 并修改相应的值
# ===========================================

# ============= 基础服务配置 =============
# 运行环境：development（开发）/ production（生产）
NODE_ENV=development

# 服务监听端口（默认3000）
PORT=3000

# 服务监听地址（默认0.0.0.0，支持localhost和127.0.0.1访问）
# 本地开发可设置为：localhost 或 127.0.0.1 或 0.0.0.0
# 云部署必须使用：0.0.0.0
HOST=0.0.0.0

# ============= 数据库配置 =============
# 数据库提供商类型：local（本地）/ 1panel（面板部署）/ cloud（云数据库）
MONGO_PROVIDER=local

# MongoDB 连接信息
MONGO_HOST=127.0.0.1
MONGO_PORT=27017
MONGO_USERNAME=your_username_here
MONGO_PASSWORD=your_secure_password_here
MONGO_DATABASE=examinationsystem

# 云数据库URI（当MONGO_PROVIDER=cloud时使用）
# MONGO_CLOUD_URI=mongodb+srv://username:password@cluster.mongodb.net/examinationsystem?retryWrites=true&w=majority

# ============= 微信小程序配置 =============
# 从微信公众平台获取
WECHAT_APPID=wx1234567890abcdef
WECHAT_SECRET=abcdef1234567890abcdef1234567890

# ============= AI模型配置 =============
# 阿里云大模型API密钥（用于智能问答等功能）,不配置将报错
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============= 安全配置 =============
# JWT签名密钥（建议使用强随机字符串）//已经配置好，初始为const secret = "qijundawang" 
JWT_SECRET=your_super_secure_jwt_secret_key_here_min_32_chars

# ============= 日志配置 ============= 
# 日志级别：error, warn, info, debug
LOG_LEVEL=info

# 日志文件路径
LOG_FILE_PATH=./logs/app.log

# ============= Redis配置（可选缓存）=============
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# ============= 邮件服务配置（可选）=============
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# ============= 文件上传配置 =============
# 最大上传文件大小（字节，默认10MB）
UPLOAD_MAX_SIZE=10485760

# 允许的文件类型
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,application/pdf

# 上传文件存储路径
UPLOAD_PATH=./public/uploads

# ============= 性能配置 =============
# 数据库连接池大小
DB_POOL_SIZE=10

# 请求超时时间（毫秒）
REQUEST_TIMEOUT=30000

# ============= 安全配置增强 =============
# CORS允许的域名（生产环境廊议设置具体域名）
CORS_ORIGIN=*

# API访问频率限制（每分钟请求数）
RATE_LIMIT_PER_MINUTE=100

# ============= 开发配置 =============
# 开发环境下的调试配置
DEBUG=app:*

# 是否启用热重载
HOT_RELOAD=true